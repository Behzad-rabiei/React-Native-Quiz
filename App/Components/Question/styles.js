import EStyleSheet from 'react-native-extended-stylesheet';

export default EStyleSheet.create({
  container: {
    marginTop: 20,
  },
  text: {
    textAlign: 'center',
    fontSize: 20,
    fontWeight: '600',
    letterSpacing: -0.02,
    color: '$white',
  },
});
